export interface FeedSelectors {
  title: {
    query: string;
    attribute: string;
  };
  author: {
    query: string;
    attribute: string;
  };
  link: {
    query: string;
    attribute: string;
  };
}
